services:
  docs:
    image: quran_backend_local_docs
    container_name: quran_backend_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./quran_backend:/app/quran_backend:z
    ports:
      - '9000:9000'
    command: /start-docs
